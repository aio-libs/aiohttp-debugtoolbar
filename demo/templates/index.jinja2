<!DOCTYPE html>
<html>
    <head>
        <title>{{ title }}</title>
        <script data-main="/static/main.js"
            src="/static/require-1.0.6.js"></script>
    </head>
    <body>
        <h1>{{ title }}</h1>
        <p>
          <a href="{{ app.router['test_redirect'].url_for() }}">Redirect example</a>
        </p>
        <p>
          <a href="{{ app.router['test_exc'].url_for() }}">Exception example</a>
        </p>
        <p>
          <a href="#'].url()}">High-order characters in URL example</a>
        </p>
        {% if show_sqla_link %}
        <p>
          <a href="#">SQLAlchemy example</a>
        </p>
        {% endif %}
        <p>
            <a href="{{ app.router['test_ajax'].url_for() }}">AJAX example</a>
        </p>

        <p>
          <a href="{{ app.router['test_jinja2_exc'].url_for() }}">Jinja2 Exception example</a>
        </p>
        {% if aiohttp_mako %}
        <p>
        <a href="{{ app.router['test_mako_exc'].url_for() }}">Mako Exception example</a>
        </p>
        {% else %}
        <p>
          aiohttp_mako is not available, please install it by <code><pre>$ pip install aiohttp_mako</pre></code> to see how mako excetion handling works.
        </p>
        {% endif %}

    </body>
</html>
